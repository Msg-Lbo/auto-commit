{
  "name": "auto-commit",
  "displayName": "自动提交消息生成器",
  "description": "使用DeepSeek AI智能生成Git提交消息的VSCode插件",
  "version": "0.0.1",
  "publisher": "Msg-Lbo",
  "author": {
    "name": "Msg-Lbo",
    "email": "msglbo@foxmail.com"
  },
  "icon": "icon.png",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "AI",
    "Other",
    "SCM Providers"
  ],
  "keywords": [
    "git",
    "commit",
    "ai",
    "deepseek",
    "自动提交",
    "提交消息",
    "人工智能"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/Msg-Lbo/auto-commit.git"
  },
  "bugs": {
    "url": "https://github.com/Msg-Lbo/auto-commit/issues"
  },
  "homepage": "https://github.com/Msg-Lbo/auto-commit#readme",
  "license": "MIT",
  "activationEvents": [
    "onView:scm"
  ],

  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "auto-commit.generateMessage",
        "title": "生成提交消息",
        "icon": "$(sparkle)"
      },
      {
        "command": "auto-commit.quickGenerate",
        "title": "快速生成提交消息"
      }
    ],
    "keybindings": [
      {
        "command": "auto-commit.generateMessage",
        "key": "ctrl+shift+alt+g",
        "when": "scmProvider == git"
      }
    ],
    "menus": {
      "scm/title": [
        {
          "command": "auto-commit.generateMessage",
          "when": "scmProvider == git",
          "group": "navigation@1"
        }
      ],
      "scm/change/title": [
        {
          "command": "auto-commit.generateMessage",
          "when": "scmProvider == git",
          "group": "inline@1"
        }
      ],
      "view/title": [
        {
          "command": "auto-commit.generateMessage",
          "when": "view == scm",
          "group": "navigation@1"
        }
      ],
      "commandPalette": [
        {
          "command": "auto-commit.generateMessage",
          "when": "gitOpenRepositoryCount != 0"
        }
      ]
    },
    "configuration": {
      "title": "自动提交消息生成器",
      "properties": {
        "auto-commit.deepseekApiKey": {
          "type": "string",
          "default": "",
          "description": "DeepSeek API密钥，用于生成提交消息",
          "scope": "application"
        },
        "auto-commit.defaultCommitTemplate": {
          "type": "string",
          "default": "feat: {description}",
          "description": "当没有历史记录时使用的默认提交消息模板",
          "scope": "application"
        },
        "auto-commit.systemPrompt": {
          "type": "string",
          "editPresentation": "multilineText",
          "default": "你是一个专业的Git提交消息生成助手。请根据代码变更生成简洁、准确的提交消息。遵循约定式提交格式（feat:, fix:, docs:, style:, refactor:, test:, chore:）。提交消息的第一行应控制在72个字符以内。",
          "description": "AI系统提示词，定义AI的角色和行为",
          "scope": "application"
        },
        "auto-commit.userPromptTemplate": {
          "type": "string",
          "editPresentation": "multilineText",
          "default": "请为以下代码变更生成一个提交消息：\n\n文件变更：\n{files}\n\n变更详情：\n{changes}\n\n历史提交消息参考：\n{history}\n\n请生成一个简洁、准确的提交消息，只返回消息内容，不要其他解释。",
          "description": "用户提示词模板，支持变量：{files}, {changes}, {history}",
          "scope": "application"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.74.0",
    "@types/node": "16.x",
    "typescript": "^4.9.4"
  },
  "extensionDependencies": [
    "vscode.git"
  ],
  "dependencies": {
    "axios": "^1.6.0",
    "simple-git": "^3.20.0"
  }
}
