{
  "name": "auto-commit",
  "displayName": "AI提交消息生成器",
  "description": "使用DeepSeek AI智能生成Git提交消息的VSCode插件",
  "version": "1.0.0",
  "publisher": "Msg-Lbo",
  "author": {
    "name": "Msg-Lbo",
    "email": "msglbo@foxmail.com"
  },
  "icon": "logo.png",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "AI",
    "Other",
    "SCM Providers"
  ],
  "keywords": [
    "git",
    "commit",
    "ai",
    "deepseek",
    "自动提交",
    "提交消息"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/Msg-Lbo/auto-commit.git"
  },
  "bugs": {
    "url": "https://github.com/Msg-Lbo/auto-commit/issues"
  },
  "homepage": "https://github.com/Msg-Lbo/auto-commit#readme",
  "license": "MIT",
  "activationEvents": [
    "onCommand:auto-commit.generateMessage",
    "onCommand:auto-commit.quickGenerate"
  ],

  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "auto-commit.generateMessage",
        "title": "生成提交消息",
        "icon": "$(sparkle)"
      },
      {
        "command": "auto-commit.quickGenerate",
        "title": "快速生成提交消息"
      }
    ],
    "keybindings": [
      {
        "command": "auto-commit.generateMessage",
        "key": "ctrl+shift+alt+g",
        "when": "scmProvider == git"
      }
    ],
    "menus": {
      "scm/title": [
        {
          "command": "auto-commit.generateMessage",
          "when": "scmProvider == git",
          "group": "navigation@1"
        }
      ],
      "scm/change/title": [
        {
          "command": "auto-commit.generateMessage",
          "when": "scmProvider == git",
          "group": "inline@1"
        }
      ],
      "view/title": [
        {
          "command": "auto-commit.generateMessage",
          "when": "view == scm",
          "group": "navigation@1"
        }
      ],
      "commandPalette": [
        {
          "command": "auto-commit.generateMessage",
          "when": "gitOpenRepositoryCount != 0"
        }
      ]
    },
    "configuration": {
      "title": "自动提交消息生成器",
      "properties": {
        "auto-commit.deepseekApiKey": {
          "type": "string",
          "default": "",
          "description": "DeepSeek API密钥，用于生成提交消息",
          "scope": "application"
        },
        "auto-commit.defaultCommitTemplate": {
          "type": "string",
          "default": "feat: {description}",
          "description": "当没有历史记录时使用的默认提交消息模板",
          "scope": "application"
        },
        "auto-commit.systemPrompt": {
          "type": "string",
          "editPresentation": "multilineText",
          "default": "你是一个专业的Git提交消息生成助手。请根据文件的实际变更状态生成一条统一的提交消息。\\n\\n格式要求：\\n- 只生成一条提交消息，不要生成多条\\n- 用逗号分隔多个变更内容\\n- 控制在200个字符以内\\n- 格式：type: 主要变更描述，次要变更1，次要变更2\\n\\n类型选择：\\n- 如果有新文件，优先使用 feat:\\n- 如果只有修改文件，使用 fix: 或 refactor: 或 style:\\n- 如果有删除文件，使用 chore:\\n\\n重要：只返回一条完整的提交消息！",
          "description": "AI系统提示词，定义AI的角色和行为",
          "scope": "application"
        },
        "auto-commit.userPromptTemplate": {
          "type": "string",
          "editPresentation": "multilineText",
          "default": "请根据以下代码变更生成一条统一的Git提交消息：\\n\\n文件变更：\\n{files}\\n\\n变更详情：\\n{changes}\\n\\n历史提交消息参考：\\n{history}\\n\\n重要要求：\\n1. 只生成一条提交消息，绝对不要生成多条消息\\n2. 用逗号分隔多个变更内容\\n3. 控制在200个字符以内\\n4. 格式：type: 主要变更描述，次要变更1，次要变更2\\n5. 如果有新文件优先用feat:，只有修改文件用fix:/refactor:/style:\\n6. 只返回一条完整的提交消息，不要任何解释或多条消息",
          "description": "用户提示词模板，支持变量：{files}, {changes}, {history}",
          "scope": "application"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "webpack --mode production --devtool hidden-source-map",
    "compile-web": "webpack",
    "watch-web": "webpack --watch",
    "package-web": "webpack --mode production --devtool hidden-source-map"
  },
  "devDependencies": {
    "@types/vscode": "^1.74.0",
    "@types/node": "16.x",
    "typescript": "^4.9.4",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4",
    "ts-loader": "^9.5.1"
  },
  "extensionDependencies": [
    "vscode.git"
  ],
  "dependencies": {
    "axios": "^1.6.0",
    "simple-git": "^3.20.0"
  }
}
